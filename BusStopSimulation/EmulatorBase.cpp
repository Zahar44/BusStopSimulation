#include "EmulatorBase.h"

EmulatorBase::EmulatorBase()
{
	/*
	if (!buses.size())
		buses.clear();
	if (!busStations.size())
		busStations.~Arr();
	Human::setBecomeChanse(5, DayTime::morning);
	Human::setBecomeChanse(7, DayTime::noonday);
	Human::setBecomeChanse(10, DayTime::evening);
	Human::setBecomeChanse(15, DayTime::night);

	auto route = new Arr<int>();
	route->push_back(1);
	route->push_back(2);
	route->push_back(3);
	buses.insert(
		std::pair<int, std::pair<Arr<Bus>, size_t>>(
			1,
			std::pair<Arr<Bus>, size_t>(*(new Bus(*route)), 3)) 
	);
	for (size_t i = 0; i < 3; i++)
		busStations.push_back(*(new BusStation()));
	*/
}
